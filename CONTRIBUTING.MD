# 为 RSS Dashboard CN 贡献

感谢你改进 RSS Dashboard CN。这个项目处理订阅地址、X 账号与关键词、文章标题和本地知识库路径；提交代码、测试或问题报告时，隐私边界与功能正确性同样重要。

## 分支与提交流程

- `main` 是准备合并和发布的主分支。
- 日常开发使用 `codex/*` 分支，例如 `codex/fix-refresh-state`。
- 从最新 `main` 创建分支，在分支上完成修改、测试和自查，再通过评审合入 `main`。
- 不要把未验证的开发提交直接当作发行版本，也不要在未经维护者确认时创建标签或发布附件。
- 提交信息使用仓库现有的约定式前缀，例如 `feat:`、`fix:`、`docs:`、`test:`、`ci:`。

## 本地环境

项目使用 Node.js 22。首次检出或依赖变化后运行：

```bash
npm ci
```

开发构建：

```bash
npm run dev
```

提交前的最低验证：

```bash
npm run check
git diff --check
```

`npm run check` 包含单元测试、本地化审计、代码检查和生产构建。若修改只覆盖特定模块，也应先运行对应的聚焦测试，再运行完整检查。

公共仓库安全验证器合入后，提交或发行准备还必须运行：

```bash
npm run check:public
npm run check:version
```

发行候选还应验证三个独立安装文件，不能只确认本地开发构建成功。

## 测试与实现要求

- 行为变化应有能够先失败、修复后通过的测试。
- 新增中英文界面文字时同时维护 `src/i18n/zh-cn.ts`、`src/i18n/en.ts` 和类型目录。
- 不要绕过每日刷新账本、TikHub 请求预算、AI 单条快照、非覆盖式 Markdown 保存或外部密钥存储。
- 修复存储问题时要覆盖中断、重复执行、并发写入和恢复副本场景。
- 不要让普通 RSS/网站/YouTube 采集依赖 TikHub 或 AI 配置。
- AI 功能必须保持用户手动选择单条信息、明确确认、单一连接调用；不得加入自动摘要、自动翻译、自动排名或隐式故障转移。

## 隐私与测试夹具

只能提交经过清洗、可以公开的合成测试数据。夹具中的域名、账号、关键词、标题、正文和文件路径必须是虚构数据，并且不能从真实响应只删掉一两个字段后直接提交。

禁止提交或粘贴：

- TikHub、AI、中转站或其他服务的 API 密钥、Token、Authorization Header；
- 完整 `data.json`、`.rss-dashboard-data`、外部 `secrets.json` 或诊断导出；
- 原始 TikHub 响应、原始模型响应、真实 Feed 导出或 OPML；
- 真实 X 账号、关注关键词、文章标题、保存笔记正文；
- 本机知识库路径、用户目录路径或能识别个人设备的日志；
- 含密钥的 `.env`、本地配置、截图或录屏。

问题报告应使用结构化错误码、插件版本、Obsidian 版本、操作系统类型和来源种类等最小信息。若必须提供诊断内容，先在插件中预览安全诊断，再人工复核后分享；不要附上完整原始日志。

## 设置导入导出边界

- 导出实现必须采用字段白名单，不能先复制完整设置再删除已知秘密字段。
- 公共设置和便携包不能包含密钥、密钥文件路径、请求历史、采集正文、AI 结果、保存笔记或带私人 URL 的错误文本。
- 包含订阅定义的导出仍可能含 Feed 地址、X 账号、关键词和文件夹名，文档与界面必须提醒用户在公开分享前复核。
- 导入的 TikHub 和 AI 连接必须断开本机密钥绑定并保持禁用或未配置。

## Pull Request 自查

提交评审前请确认：

- [ ] 聚焦测试和 `npm run check` 通过；
- [ ] 中英文文本同时更新，`npm run audit:i18n` 通过；
- [ ] 没有真实密钥、响应、订阅、账号、关键词、标题或本地路径；
- [ ] 新测试夹具完全合成并经过清洗；
- [ ] 数据目录、外部联网或隐私边界发生变化时，README 和隐私文档已同步；
- [ ] 明确说明是否需要发行说明、迁移说明或版本更新；
- [ ] 复用了上游代码或设计时，在提交说明中写明来源和适用的 MIT 署名；
- [ ] 没有把上游资助、社区或发布入口描述为本衍生版本的入口。

## 发行说明与上游署名

每项用户可见变化都要在评审说明中给出一条简明发行说明；无须写入发行说明时也要明确标注原因。涉及设置结构、存储格式、默认路径、联网请求、计费边界或卸载清理的变化，必须附迁移和回退说明。

本项目从 [Aditya Amatya 的 Obsidian RSS Dashboard](https://github.com/amatya-aditya/obsidian-rss-dashboard) `2.5.0` 开始衍生。保留 `LICENSE` 中的上游 MIT 许可文本；新增或移植上游实现时，不得删除相应版权与许可证信息。

## 安全问题

不要在公开问题中披露凭证或私人数据。公开仓库的私密漏洞报告渠道准备完成前，请只提交不含敏感信息的最小复现描述，并等待维护者提供安全的后续渠道。
