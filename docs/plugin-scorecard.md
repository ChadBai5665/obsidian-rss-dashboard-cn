# RSS Dashboard CN 0.1.0 验证状态

本文件只记录当前衍生版本的验证事实，不复用上游插件的社区目录评分、安装量、Issue 响应率或资助状态。

| 检查项 | 状态 | 证据边界 |
|---|---|---|
| 单元测试、本地化审计、代码检查与生产构建 | 已验证 | 本地 `npm run check` 通过 |
| 插件 ID、作者与 `0.1.0` 元数据 | 已验证 | 本地静态检查通过 |
| 中文安装、隐私、故障排查和上游署名文档 | 已验证 | 文档测试与人工核对 |
| Obsidian 社区插件目录审核 | 尚未验证 | 当前仅手动自安装，未宣称已上架 |
| 真实 Obsidian 图形界面冒烟测试 | 已验证（macOS） | Obsidian 1.12.7 中完成安装、设置、信息台、阅读器、字幕缓存重开与正常重启复验；不代表 Windows/Linux 运行时 |
| TikHub 真实密钥和费用路径 | 部分验证 | 已使用现有外部密钥完成 YouTube 字幕回退；TikHub X 账号/主题实时路径仍未在本表声明通过 |
| TikHub YouTube 字幕真实费用路径 | 已验证（单一样本） | 历史基线为 Task 10 前未运行；2026-07-30 对公开视频 `bWR_3Sg-H-8` 一次明确点击后取得非空人工英文字幕；本地账本确认 2 次请求、预计约 `$0.016`。缓存重开和 Obsidian 重启后账本未增加；最终计费以服务商账单为准 |
| TikHub 字幕供 AI 被动读取 | 已验证（无模型调用） | 本地测试确认 `provider=tikhub` 的 `youtube-transcript` 缓存被内容选择器优先读取且不触发全文抓取；UI 只打开并取消 AI 操作菜单，未发送模型请求 |
| AI 服务商真实连接 | 尚未验证 | 未提供一次性测试密钥，未调用可能计费的接口 |
| Windows 与 Linux 运行时 | 尚未验证 | 当前只核对代码路径和自动化测试 |
| 公开仓库安全扫描 | 已验证 | 当前 `npm run check:public` 通过；结论限于仓库内现有扫描规则，不等同于第三方安全审计 |
| 发行附件完整性检查 | 已验证 | 当前 `npm run release:check` 通过，确认本地暂存发布文件有效；不代表已上传发行附件或通过社区目录审核 |

“已验证”只适用于表中列出的证据，不代表第三方接口持续可用，也不代表已经通过 Obsidian 社区审核。
