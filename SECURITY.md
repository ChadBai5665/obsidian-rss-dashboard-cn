# 安全策略

## 支持范围

当前仅维护 `RSS Dashboard CN 0.1.x`。上游 `Obsidian RSS Dashboard` 的安全问题应提交给上游项目；本分支新增的采集、TikHub、AI、外部密钥和本地存储问题由本仓库处理。

## 私密报告漏洞

不要通过公开 Issue、Discussion、评论或截图披露漏洞细节。

公开仓库建立后，仓库所有者应启用 GitHub Private vulnerability reporting。报告者请在仓库的 **Security → Advisories → Report a vulnerability** 中提交私密报告，由仓库所有者在 GitHub 的私密通道中回复。

在公开仓库和上述私密通道建立前，请不要公开漏洞细节，也不要自行猜测或寻找个人邮箱。待仓库上线后，通过其私密漏洞报告入口联系仓库所有者。

报告请包含：

- 受影响的插件、Obsidian 和操作系统版本；
- 最小复现步骤、实际影响和预期行为；
- 是否可能读取密钥、覆盖文件、泄露私人来源或产生计费请求；
- 使用合成数据得到的脱敏日志或最小复现文件。

## 禁止提交的内容

无论公开还是私密报告，都不要直接粘贴：

- API 密钥、Authorization/Cookie 请求头或完整外部密钥文件；
- 完整 `data.json`、订阅源导出、OPML、原始 TikHub 响应或 AI 响应；
- 真实 feed URL、X 账号、关注关键词、知识库路径、用户目录或保存文章正文；
- 能还原个人订阅、阅读记录或本地文件结构的截图和日志。

请用合成值替换敏感内容，并保留字段结构、错误码和必要的时间顺序。若漏洞本身涉及密钥，请撤销或轮换受影响密钥；不要把真实密钥作为复现材料。

## 协调披露

请在维护者确认影响、准备修复并完成验证前保持私密。维护者会在私密报告中说明处理状态；修复发布后再协调公开说明。不要把未验证的修复描述为已发布版本。
